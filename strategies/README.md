# strategies/

Entities of `type: strategy` — published strategic plans (e.g. a national
data strategy). Distinct from `programme` (a funded, time-bound delivery
vehicle) and `policy` (a non-binding position).

Filename: `<id-lowercased>.md`. Template: `templates/entity-template.md`.
