# standards/

Entities of `type: standard` — technical or semantic standards. Connect each
standard to its maintaining organisation via `maintained-by` and/or the
`organisations:` list (`metadata/relationship-types.md`).

Filename: `<id-lowercased>.md`. Template: `templates/entity-template.md`.
