# frameworks/

Entities of `type: framework` — architecture or governance frameworks that
organise standards, policies or programmes (e.g. an enterprise architecture
framework). Distinct from `standard` (a specific technical/semantic spec).

Filename: `<id-lowercased>.md`. Template: `templates/entity-template.md`.
