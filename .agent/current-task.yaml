# In-flight task tracker. Read alongside .agent/state.yaml at the start
# of every session. `status: idle` means no batch is in progress — pick
# the next item per .agent/operating-model.md's priority order rather
# than waiting to be told what to do.
#
# When starting a multi-step task that might get interrupted (timeout,
# crash), set status to "in_progress" and fill in the fields below in the
# same commit as the first step, so a future session can resume cleanly
# instead of re-discovering what was happening. Set it back to "idle"
# (and clear the fields) once the work is merged.

status: idle

task: null
branch: null
started: null
description: null
next_step: null

notes: >
  Idle as of 2026-09-26. A long continuous run (PRs #347-366) closed
  several discovery/unresolved.md rows, added new entities (BE-CYFUN,
  GB-NATIONAL-DATA-LIBRARY, DE-VDE) and fixed a run of stale
  cross-references found by grepping for "not modelled"/"unmodelled"/
  "logged in research-queue.md" phrasing across the repo -- a
  productive pattern worth repeating in a future session, since several
  entities created mid-session leave sibling files with stale "X is not
  modelled" claims that nothing currently checks automatically.
  Stopped 2026-09-26 per explicit user instruction ("Stop at next
  pass") after PR #366 merged -- do not pick up further work
  automatically; resume only when asked. Several remaining
  Known-source-block rows (bmi.bund.de, digitale-verwaltung.de,
  geant.org family) were retried 2026-09-26 and confirmed still blocked
  -- no need to retry again soon.
